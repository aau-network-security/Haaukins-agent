syntax = "proto3";
package agent;
option go_package = "github.com/aau-network-security/haaukins-agent/proto";

//TODO add CreateLabsForEnv function
service Agent {
    rpc Init(InitRequest) returns (StatusResponse) {}
    rpc CreateEnvironment(CreatEnvRequest) returns (StatusResponse) {}
    rpc CreateLabForEnv(CreateLabRequest) returns (StatusResponse) {}
    rpc AddExercisesToLab(AddExerciseRequest) returns (StatusResponse) {}
    rpc LabStream(Empty) returns (stream Lab) {}
}

message Empty{}

message InitRequest {
    string url = 1;
    string authKey = 2;
    string signKey = 3;
    bool tlsEnabled = 4;
}

message CreatEnvRequest {
    string eventTag = 1;
    int32 envType = 2;
    repeated VmConfig vms = 3;
    int32 initialLabs = 4;
    repeated string exercises = 5;
}

message CreateLabRequest{
    string eventTag = 1;
    bool isVPN = 2;
}

message AddExerciseRequest {
    string labTag = 1;
    repeated string exercises = 2;
}

message VmConfig {
    string image = 1;
    uint32 memoryMB = 2;
    double cpu = 3;
}

message StatusResponse {
    string message = 1;
}

message Lab {
    string tag = 1;
    string eventTag = 2;
    repeated Exercise exercises = 3;
    uint32 guacPort = 4;
    bool isVPN = 5;
}

message Exercise {
    string tag = 1;
    repeated string flags = 2;
    repeated string errors = 3;
}